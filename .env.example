# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/yalla_interview

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_here_at_least_32_characters

# AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:3000

# Security Notes:
# - Use a strong JWT_SECRET with at least 32 characters
# - Set NODE_ENV=production for production deployment
# - Update FRONTEND_URL to your actual frontend domain in production
# - Keep this file secure and never commit actual secrets to version control
